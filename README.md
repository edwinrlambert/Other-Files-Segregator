# Other Files Segregator

This utility organizes files in a specified directory based on their mime type. Files are moved into directories with a numbered prefix to group similar file types together.

